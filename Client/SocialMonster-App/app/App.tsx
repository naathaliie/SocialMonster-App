/* import React from "react";
import { Provider } from "react-redux";
import { store } from "../redux/store"; // Importera din store
import RootLayout from "../components/"; // Importera RootLayout

// Här omsluter vi hela applikationen med Provider
export default function App() {
  return (
    <Provider store={store}> 
      <RootLayout /> 
    </Provider>
  );
}
 */
